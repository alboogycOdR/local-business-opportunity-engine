[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "lboe-maps-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27", "lboe-domain"]

[tool.hatch.build.targets.wheel]
packages = ["src/lboe_maps_scraper"]
