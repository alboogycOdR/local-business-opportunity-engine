# Docker

Root `docker-compose.yml` currently provides PostgreSQL and Redis only.
Sprint 1 may extend local containers if that materially improves reproducibility, but should avoid unnecessary infrastructure.
